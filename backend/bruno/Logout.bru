meta {
  name: Logout
  type: http
  seq: 18
}

post {
  url: {{base_url}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Logout successful", function() {
    expect(res.body.message).to.include("Déconnexion");
  });
}
